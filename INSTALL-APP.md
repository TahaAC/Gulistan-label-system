# Gulistan Price Tags - Desktop App Installation

## 📱 Install as Desktop App (PWA)

Your barcode label system can now be installed as a desktop application!

### Installation Steps:

#### Chrome / Edge (Windows/Mac):
1. Open `standalone.html` in Chrome or Edge browser
2. Look for the **install icon** (➕ or ⬇️) in the address bar
3. Click it and select **"Install"**
4. The app will open in its own window
5. Find the app in your Start Menu / Applications

#### Manual Installation:
1. Open Chrome and go to: `chrome://apps`
2. Click the menu (⋮) → **"Install this site as an app"**
3. Name it "Gulistan Price Tags" and click Install

### Features When Installed:
- ✅ Runs in standalone window (no browser tabs)
- ✅ Desktop shortcut created automatically
- ✅ Works offline with cached data
- ✅ Faster performance
- ✅ Full camera access for barcode scanning
- ✅ Access from Start Menu / Dock

### Camera Permissions:
⚠️ **Important**: For camera scanning to work:
- Use **HTTPS** or **localhost**
- Allow camera permissions when prompted
- Or use browser "Insecure origins treated as secure" flag:
  - Chrome: `chrome://flags/#unsafely-treat-insecure-origin-as-secure`
  - Add: `http://localhost` or your file path

### Uninstall:
- **Windows**: Settings → Apps → Gulistan Price Tags → Uninstall
- **Mac**: Right-click app in Applications → Move to Trash
- **Chrome**: chrome://apps → Right-click → Remove from Chrome

---

## 🚀 Quick Start:
1. Open `standalone.html`
2. Click "Install" when prompted
3. Start scanning barcodes with camera!

**Note**: The app works both installed and as a regular webpage.
